<% provide(:title, 'Home') %>

  <div class="page-header">
  </div>
  <div class="center emertron">
   <!-- image displayed via css, this was an option as heroku wasn't picking up img
    <img src="../images/studio-in-action-sml.jpg" alt="Potters working studio" title="Potters Craft Studio" >
   -->
  </div>
  <div class ="homepage-container center">
    <h1 class="center">Welcome to Craft<span class="myLink">Work</span></h1>
    
    <% if user_signed_in? %>
      <%= link_to "Sign Out", destroy_user_session_path, class: "btn btn-lg btn-custom", method: :delete %>
    <% else %>
      <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-lg btn-custom" %>
    <% end %>
  </div>
 